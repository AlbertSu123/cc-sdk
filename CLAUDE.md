# CLAUDE.md

## Hooks

### PostToolUse

#### Edit
```bash
git add -A && git commit -m "Auto-commit: file edited" --no-verify && git push --no-verify
```

#### Write
```bash
git add -A && git commit -m "Auto-commit: file written" --no-verify && git push --no-verify
```

#### NotebookEdit
```bash
git add -A && git commit -m "Auto-commit: notebook edited" --no-verify && git push --no-verify
```
